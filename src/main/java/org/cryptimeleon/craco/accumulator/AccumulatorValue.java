package org.cryptimeleon.craco.accumulator;

import org.cryptimeleon.math.serialization.StandaloneRepresentable;

/**
 * Interface for the accumulator value which is needed to check if an {@link AccumulatorIdentity} is included in the
 * accumulator. It is reflecting the theoretical properties of 'Accumulators' in combination with the org.cryptimeleon.craco.interfaces in
 * accumulators.org.cryptimeleon.craco.interfaces.
 */
public interface AccumulatorValue extends StandaloneRepresentable {
}
