package org.cryptimeleon.craco.accumulator;

import org.cryptimeleon.math.serialization.StandaloneRepresentable;

/**
 * Interface for one identity that can be accumulated in a 'Accumulator'. Furthermore, its
 * purpose is to reflect the theoretical properties of 'Accumulators' in combination with the org.cryptimeleon.craco.interfaces in
 * accumulators.org.cryptimeleon.craco.interfaces:
 */
public interface AccumulatorIdentity extends StandaloneRepresentable {
}
