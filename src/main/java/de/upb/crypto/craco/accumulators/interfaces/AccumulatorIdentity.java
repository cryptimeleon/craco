package de.upb.crypto.craco.accumulators.interfaces;

import de.upb.crypto.math.serialization.StandaloneRepresentable;

/**
 * Interface for one identity that can be accumulated in a 'Accumulator'. Furthermore, its
 * purpose is to reflect the theoretical properties of 'Accumulators' in combination with the interfaces in
 * accumulators.interfaces:
 */
public interface AccumulatorIdentity extends StandaloneRepresentable {
}
