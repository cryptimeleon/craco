package de.upb.crypto.craco.accumulators.interfaces;

import de.upb.crypto.math.serialization.StandaloneRepresentable;

/**
 * Interface for the accumulator value which is needed to check if an {@link AccumulatorIdentity} is included in the
 * accumulator. It is reflecting the theoretical properties of 'Accumulators' in combination with the interfaces in
 * accumulators.interfaces.
 */
public interface AccumulatorValue extends StandaloneRepresentable {
}
